<UserControl x:Class="Camara_Service.InfoBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Height="Auto" >
    <Border CornerRadius="20" Padding="5" Background="#2C2C3E" Margin="4" ScrollViewer.CanContentScroll="True">
        <ScrollViewer VerticalScrollBarVisibility="Hidden">
            <Grid>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Horloge -->
                    <Grid Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="10">
                        <Ellipse Width="110" Height="110" Stroke="#FF9800" StrokeThickness="4">
                            <Ellipse.Effect>
                                <DropShadowEffect BlurRadius="15" Color="#FF9800" ShadowDepth="0"/>
                            </Ellipse.Effect>
                        </Ellipse>
                        <TextBlock x:Name="ClockText"
                            Text="12:45:00"
                            Foreground="White"
                            FontSize="22"
                            FontWeight="Bold"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>
                    </Grid>

                    <!-- Infos Sync -->
                    <StackPanel Grid.Row="1" Margin="0,15,0,10" VerticalAlignment="Top" HorizontalAlignment="Center">
                        <TextBlock Foreground="#FF9800" FontSize="16" FontWeight="Bold" TextAlignment="Center" MouseUp="TextBlock_MouseUp"><Run Text="Dernière s"/><Run Text="auvegarde"/><Run Text=" :"/></TextBlock>
                        <TextBlock x:Name="LastSyncText" Text="Jamais" Foreground="White" FontSize="14" TextAlignment="Center"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                            <Button Cursor="Hand" Click="CloudSync_Click">
                                <!-- Supprimer le style par défaut -->
                                <Button.Template>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Button.Template>

                                <!-- Animations -->
                                <Button.RenderTransform>
                                    <TransformGroup>
                                        <ScaleTransform x:Name="IconScale" ScaleX="1" ScaleY="1"/>
                                        <RotateTransform x:Name="IconRotate" Angle="0"/>
                                    </TransformGroup>
                                </Button.RenderTransform>

                                <Button.Triggers>
                                    <!-- Hover Enter -->
                                    <EventTrigger RoutedEvent="UIElement.MouseEnter">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <!-- Zoom -->
                                                <DoubleAnimation Storyboard.TargetName="IconScale"
                                                    Storyboard.TargetProperty="ScaleX"
                                                    To="1.2" Duration="0:0:0.2"/>
                                                <DoubleAnimation Storyboard.TargetName="IconScale"
                                                    Storyboard.TargetProperty="ScaleY"
                                                    To="1.2" Duration="0:0:0.2"/>
                                                <!-- Rotation -->
                                                <DoubleAnimation Storyboard.TargetName="IconRotate"
                                                    Storyboard.TargetProperty="Angle"
                                                    To="15" Duration="0:0:0.2"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                    <!-- Hover Leave -->
                                    <EventTrigger RoutedEvent="UIElement.MouseLeave">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <!-- Retour Zoom -->
                                                <DoubleAnimation Storyboard.TargetName="IconScale"
                                                    Storyboard.TargetProperty="ScaleX"
                                                    To="1" Duration="0:0:0.2"/>
                                                <DoubleAnimation Storyboard.TargetName="IconScale"
                                                    Storyboard.TargetProperty="ScaleY"
                                                    To="1" Duration="0:0:0.2"/>
                                                <!-- Retour Rotation -->
                                                <DoubleAnimation Storyboard.TargetName="IconRotate"
                                                    Storyboard.TargetProperty="Angle"
                                                    To="0" Duration="0:0:0.2"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Button.Triggers>

                                <!-- Icône -->
                                <materialDesign:PackIcon Kind="CloudSync" Width="28" Height="28" Foreground="#FF9800"/>
                            </Button>

                            <TextBlock x:Name="SyncStatus" Text="Sync" Foreground="White" FontSize="14" Margin="5,0"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Stats Rapides -->
                    <StackPanel Grid.Row="2" Orientation="Vertical" VerticalAlignment="Top" HorizontalAlignment="Center">
                        <!-- Aujourd'hui -->
                        <Border Background="#1E1E2F" CornerRadius="12" Padding="10"  MinWidth="170"  MinHeight="101" Margin="0,5,0,5">
                            <StackPanel>
                                <TextBlock Text="Cette Semaine" Foreground="#FF9800" FontWeight="Bold" FontSize="14"/>
                                <TextBlock x:Name="TodaySales" Text="15 Factures" Foreground="White" FontSize="16" FontWeight="Bold"/>
                                <TextBlock x:Name="TodayAmount" Text="1,250 FCFA" Foreground="White" FontSize="14"/>
                                <TextBlock x:Name="TodayAmount_Copy" Text="1,250 FCFA" Foreground="White" FontSize="14"/>
                                <TextBlock x:Name="TodayAmount_Copy1" Text="1,250 FCFA" Foreground="White" FontSize="14"/>
                            </StackPanel>
                        </Border>

                        <!-- Hier -->
                        <Border Background="#1E1E2F" CornerRadius="12" Padding="10"  MinWidth="170" MinHeight="110" Margin="0,5,0,5">
                            <StackPanel>
                                <TextBlock Text="Cet Mois" Foreground="#FF9800" FontWeight="Bold" FontSize="14"/>
                                <TextBlock x:Name="YesterdaySales" Text="12 Factures" Foreground="White" FontSize="16" FontWeight="Bold"/>
                                <TextBlock x:Name="YesterdayAmount" Text="980 FCFA" Foreground="White" FontSize="14"/>
                                <TextBlock x:Name="YesterdayAmount_Copy" Text="980 FCFA" Foreground="White" FontSize="14"/>
                                <TextBlock x:Name="YesterdayAmount_Copy1" Text="980 FCFA" Foreground="White" FontSize="14"/>
                            </StackPanel>
                        </Border>

                        <!-- Cette semaine -->
                        <Border Background="#1E1E2F" CornerRadius="12" Padding="10" MinWidth="170" MinHeight="106" Margin="0,5,0,5">
                            <StackPanel>
                                <TextBlock Foreground="#FF9800" FontWeight="Bold" FontSize="14"><Run Text="Cette Ann"/><Run Text="ée"/></TextBlock>
                                <TextBlock x:Name="WeekSales" Text="70 Factures" Foreground="White" FontSize="16" FontWeight="Bold"/>
                                <TextBlock x:Name="WeekAmount" Text="5,800 FCFA" Foreground="White" FontSize="14"/>
                                <TextBlock x:Name="WeekAmount_Copy" Text="5,800 FCFA" Foreground="White" FontSize="14"/>
                                <TextBlock x:Name="WeekAmount_Copy1" Text="5,800 FCFA" Foreground="White" FontSize="14"/>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </Grid>

                <!-- Overlay semi-transparent -->
                <Grid x:Name="Overlay" Background="#88000000" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="Collapsed">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock x:Name="Etat" Text="Uploading..." Foreground="White" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>

                        <ProgressBar Width="200" Height="20" IsIndeterminate="True"/>
                    </StackPanel>
                </Grid>
                <!-- Overlay semi-transparent 2 -->
                <Grid x:Name="Overlay2" Background="#88000000" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="Collapsed">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" >
                        <TextBlock x:Name="etat2" Text="Sauvegarde terminée" Foreground="White" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
                            <!-- Icon de confirmation -->
                            <materialDesign:PackIcon x:Name="IconSuccess" Kind="CheckCircle" Width="48" Height="48" Foreground="LimeGreen" Visibility="Collapsed"/>

                            <!-- Icon d'échec -->
                            <materialDesign:PackIcon x:Name="IconError" Kind="AlertCircle" Width="48" Height="48" Foreground="Red" Visibility="Collapsed"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Grid>
        </ScrollViewer>


    </Border>
    
</UserControl>
