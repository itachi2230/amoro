<UserControl x:Class="Camara_Service.StockCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" Height="177" Width="214.678">

    <UserControl.Resources>
        <!-- Style pour Hover Effect -->
        <Style x:Key="HoverBorderStyle" TargetType="Border">
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#00CFFF" BlurRadius="15" ShadowDepth="0" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <!-- Hover -->
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <!-- Animation de Scale -->
                                <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)" To="1.05" Duration="0:0:0.3"/>
                                <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)" To="1.05" Duration="0:0:0.3"/>
                                <!-- Animation Glow -->
                                <DoubleAnimation Storyboard.TargetProperty="(Effect).Opacity" To="0.8" Duration="0:0:0.3"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.3"/>
                                <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.3"/>
                                <DoubleAnimation Storyboard.TargetProperty="(Effect).Opacity" To="0.3" Duration="0:0:0.3"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <!-- Contenu principal -->
    <Border Style="{StaticResource HoverBorderStyle}"
            CornerRadius="14" Margin="6"
            BorderBrush="#00CFFF" BorderThickness="2" 
            Background="#1B2A49">

        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header (Icon + Title) -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <materialDesign:PackIcon Kind="Store" Foreground="#00CFFF" Width="22" Height="22" Margin="0,0,6,0"/>
                <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                           Foreground="#00CFFF" FontSize="20" FontWeight="Bold"/>
            </StackPanel>

            <!-- Value (Centered) -->
            <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,10,0,0">
                <TextBlock Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                           Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock Text="Articles" Foreground="#B0BEC5" FontSize="12" HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- ProgressBar + Text -->
            <StackPanel Grid.Row="2" Margin="0,15,0,0">
                <ProgressBar x:Name="progresss" 
                             Value="{Binding Progress, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                             Maximum="100" Height="10" Width="200" 
                             Foreground="#00CFFF" Background="#2E3C57"></ProgressBar>
                <TextBlock Text="{Binding LastUpdate, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                           Foreground="#90A4AE" FontSize="12" Margin="0,8,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
